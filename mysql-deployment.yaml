apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
        - name: mysql
          image: mysql:5.7
          env:
            - name: MYSQL_DATABASE
              value: "timesheet2021"           
            - name: MYSQL_ROOT_PASSWORD
              value: ""      
            - name: MYSQL_USER
              value: "root"
            - name: MYSQL_PASSWORD
              value: ""
          ports:
            - containerPort: 3306
